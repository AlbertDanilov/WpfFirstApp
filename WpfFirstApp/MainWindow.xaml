<Window x:Class="WpfFirstApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfFirstApp" 
        xmlns:col="clr-namespace:System.Collections;assembly=mscorlib"
        mc:Ignorable="d"
        Title="окно" Height="250" Width="300">
    <Window.Resources>
        <!--<ObjectDataProvider x:Key="phonesProvider"
                ObjectType="{x:Type local:PhoneRepository}" MethodName="GetPhones" />-->
        <!--<XmlDataProvider x:Key="phonesProvider" Source="Res\phones.xml" XPath="phones"/>-->
    </Window.Resources>
    <Grid>
        <!--<ListBox x:Name="phonesList" DisplayMemberPath="title"
                 ItemsSource="{Binding Source={StaticResource phonesProvider}, XPath=phone}" />-->
        <TreeView x:Name="treeView1">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Path=Nodes}">
                    <TextBlock Text="{Binding Name}"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</Window>
